source .goobook_secrets
# "#" or ";" at the start of a line makes it a comment.
[DEFAULT]
# If not given here, email and password is taken from .netrc using
# machine google.com
email: $EMAIL
password: $PASSWORD
# or if you want to get the password from a commmand:
# passwordeval: sudo -u jfranklin /usr/bin/security -v find-generic-password -g -a jfranklin@telesphere.com -s imap.gmail.com /Users/jfranklin/Library/Keychains/login.keychain
# The following are optional, defaults are shown
;cache_filename: ~/.goobook_cache
;cache_expiry_hours: 24
;filter_groupless_contacts: yes

